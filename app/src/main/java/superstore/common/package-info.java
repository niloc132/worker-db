/**
 * Represents common GWT code, run by both app and worker. The client package is code only shared
 * by those two, the shared package includes code shared with the server as well.
 */
package superstore.common;